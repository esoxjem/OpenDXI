# GitHub Configuration
GITHUB_ORG=your-github-org

# GitHub Personal Access Token (required)
# Create at: https://github.com/settings/tokens
# Required scopes: repo, read:org
GH_TOKEN=

# Sprint Configuration
SPRINT_START_DATE=2026-01-07
SPRINT_DURATION_DAYS=14

# Database Configuration (FastAPI uses SQLite, Rails uses ActiveRecord)
DB_PATH=.data/opendxi.db
DB_TIMEOUT=30

# GitHub API Settings
MAX_PAGES_PER_QUERY=10

# Server Configuration (FastAPI legacy, kept for reference)
HOST=0.0.0.0
PORT=8000
DEBUG=True

# CORS Configuration
# For Rails: comma-separated list of origins (e.g., http://localhost:3000,https://app.example.com)
# For FastAPI (deprecated): JSON array format was used
CORS_ORIGINS=http://localhost:3000
