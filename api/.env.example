# GitHub Configuration
GITHUB_ORG=your-github-org

# GitHub Personal Access Token (required for fetching metrics)
# Create at: https://github.com/settings/tokens
# Required scopes: repo, read:org
GH_TOKEN=

# Sprint Configuration
SPRINT_START_DATE=2026-01-07
SPRINT_DURATION_DAYS=14

# ═══════════════════════════════════════════════════════════════════════════
# Local Development: Authentication Bypass
# ═══════════════════════════════════════════════════════════════════════════
# Authentication is AUTOMATICALLY bypassed in development when GitHub OAuth
# credentials are not configured. This allows local dev to work out of the box.
#
# Set SKIP_AUTH=true to explicitly bypass auth even if OAuth is configured.
# SKIP_AUTH=true

# Database Configuration (FastAPI uses SQLite, Rails uses ActiveRecord)
DB_PATH=.data/opendxi.db
DB_TIMEOUT=30

# GitHub API Settings
MAX_PAGES_PER_QUERY=10

# ═══════════════════════════════════════════════════════════════════════════
# GitHub OAuth Configuration (optional for local development)
# ═══════════════════════════════════════════════════════════════════════════
# Leave empty for local development - auth will be automatically bypassed.
# Required for production deployments.
#
# Create an OAuth App at: https://github.com/settings/developers
# See docs/github-oauth-setup.md for detailed instructions
GITHUB_OAUTH_CLIENT_ID=
GITHUB_OAUTH_CLIENT_SECRET=
GITHUB_OAUTH_CALLBACK_URL=http://localhost:3000/auth/github/callback

# ═══════════════════════════════════════════════════════════════════════════
# User Access Control
# ═══════════════════════════════════════════════════════════════════════════
# Users must be added by an owner via the Settings UI before they can log in.
# The first owner is bootstrapped via this environment variable.
#
# First user with this GitHub username becomes owner on first login
# (case-insensitive comparison). Required for initial setup.
# Once an owner exists, additional users are managed via Settings UI.
OWNER_GITHUB_USERNAME=

# Frontend URL for redirects after login/logout
FRONTEND_URL=http://localhost:3001

# ═══════════════════════════════════════════════════════════════════════════
# CORS Configuration
# ═══════════════════════════════════════════════════════════════════════════
# Comma-separated list of origins (must include frontend URL)
CORS_ORIGINS=http://localhost:3001
